# Copilot Instructions

## Project Guidelines
- For the repository's hybrid VSSDK+VisualStudio.Extensibility project built with Visual Studio 2026 tooling, do not assume every generic VSIX schema 2.0 metadata element is accepted; validate against the generated manifest/build schema. GettingStartedGuide is rejected by the active VSSDK schema.